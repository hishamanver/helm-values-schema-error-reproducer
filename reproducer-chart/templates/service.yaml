apiVersion: v1
kind: Service
metadata:
  name: {{ include "reproducer-chart.fullname" . }}
  labels:
    {{- include "reproducer-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reproducer-chart.selectorLabels" . | nindent 4 }}
