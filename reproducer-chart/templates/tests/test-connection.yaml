apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reproducer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "reproducer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reproducer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
